services:
  api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/app.db

#   redis:
#     image: redis:7
#     ports:
#       - "6379:6379"
#     volumes:
#       - redis_data:/data

#   nginx:
#     image: nginx:latest
#     ports:
#       - "80:80"
#     volumes:
#       - ./nginx.conf:/etc/nginx/nginx.conf:ro
#     depends_on:
#       - api

# volumes:
#   redis_data:
